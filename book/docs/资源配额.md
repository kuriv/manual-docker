# 资源配额

执行下面的命令，创建容器并指定资源配额。

```
docker run -d --cpus 1 -m 512m redis
```

执行下面的命令，动态更新指定容器的资源配额。

```
docker update --cpus 0.5 -m 256m CONTAINER_ID
```

执行下面的命令，实时查看容器的资源占用情况。

```
docker stats CONTAINER_ID
```

