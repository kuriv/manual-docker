# 数据卷

执行下面的命令，查看所有的数据卷。

```
docker volume ls
```

执行下面的命令，创建指定的数据卷。

```
docker volume create demo
```

执行下面的命令，查看指定数据卷的全部信息。

```
docker volume inspect demo
```

执行下面的命令，删除指定的数据卷。

```
docker volume rm demo
```

